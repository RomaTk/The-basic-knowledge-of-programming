/******************************************************************************

                              Online C++ Compiler.
               Code, Compile, Run and Debug C++ program online.
Write your code in this editor and press "Run" button to compile and execute it.

*******************************************************************************/

#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    double x=0,v27=0;
    cout<<"Введите x:";
    cin>>x;
    
    
    v27=(pow(log(sqrt(fabs(x))+pow(x,2)+sin(fabs(x))),2))/pow(log2(exp(sqrt(x))+pow(x,4)),2);
    cout<<"v27="<<v27;
    
    

    return 0;
}
