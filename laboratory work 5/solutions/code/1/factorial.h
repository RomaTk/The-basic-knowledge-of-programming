#pragma once

// value!
// Using int type because the min "long int x!">"long long int x"
// if recursion == true than factorial be calculated recursively
long long int factorial(const int& value, bool recursion = false);
